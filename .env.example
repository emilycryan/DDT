# PostgreSQL connection (used by server, pgvector, and scripts)
# Neon: use the connection string from Neon Console → Connect (pooled recommended).
# Include ?sslmode=require if your client doesn't enable SSL by default.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require

# Example Neon pooled URL (from Neon Console):
# DATABASE_URL=postgresql://alex:xxx@ep-cool-darkness-xxxxx-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require

# Optional: alternative env name (scripts may check both)
# POSTGRES_URL=postgresql://...
